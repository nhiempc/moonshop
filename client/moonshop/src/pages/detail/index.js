import React from 'react';
import Layout from '../../components/Layout';

const DetailPage = () => {
    return(
        <Layout>
            <h1>DetailPage</h1>
        </Layout>
    )
}

export default React.memo(DetailPage);